# Session Log — Rally Exterior Solutions (June 16–19, 2026)

What we accomplished this session. Durable architecture/config lives in `CLAUDE.md`;
this is the "what we did + decisions + current state" record. (No secrets here —
credential values live in Vercel env.)

## 1. Reviews & Google Business Profile
- **GBP:** claimed + verified; primary category `Pressure washing service` confirmed.
  Delivered full copy-paste optimization pack → `docs/GBP-OPTIMIZATION.md`.
  User completed: categories, service area (12 cities, hid address), hours (fixed an
  erroneous "Open 24 hours"), and photos (16 staged in `~/Desktop/Rally-GBP-Photos/`).
  Still user-side: paste description + 7 services, weekly Google Posts, seed Q&A, reply
  to all 30 reviews.
- **Reviews strategy:** low volume (~5 jobs/mo, premium) → win on **capture RATE** +
  **mining PAST customers**, not job volume. Uses **Jobber** for automated requests
  (don't rebuild that).
- **Built review tooling:** `/review` vanity short link → Google review page
  (`g.page/r/CaY2UgCpz3LrEAI/review`), a QR code (`public/review-qr.*`), and a printable
  chrome-free `/review-card` (noindex) for in-person asks / leave-behinds / past-customer
  texts. Linked from the dashboard.

## 2. SEO build-out
- Corrected review count **35 → 30** (matches GBP).
- Service area expanded to **~53 city pages** (user ran the geocoded expansion).
- **Service×city combo pages** at `/services/[slug]/[city]` — **26 curated combos**
  (lighting-weighted), unique local content, `dynamicParams=false` so only curated combos
  exist (no doorway pages). Config: `src/data/serviceAreas.ts`.
- **"Pressure & Power Washing"** umbrella service added — owns the head terms the public
  actually searches (the site was built on premium method terms).
- **6 guides** (`src/data/guides.ts`): 5 pricing (house-washing, permanent-lighting,
  christmas-light-installation, roof-cleaning, pressure-washing **cost**) + educational
  **pressure-washing-vs-soft-washing**. `getGuideForService()` drives service-page callouts.
- **Keyword data insight:** `christmas lights wheeling wv` ≈ **500/mo, LOW competition** =
  the biggest local term (seasonal Oct–Dec); **lighting carries the local volume**.
  Commercial-intent terms (pressure washing company, power washing services) + `driveway
  power wash` (+900% YoY) added to the tracker. Bare "pressure washing" (5,000/mo) is
  national/unwinnable — intentionally not chased.
- **AEO / AI search:** `robots.ts` welcomes AI crawlers (GPTBot, ClaudeBot, PerplexityBot,
  Google-Extended…); `public/llms.txt` summarizes the business for answer engines.

## 3. Dashboard (Search Console)
- Connected GSC via a **service-account JWT** (no deps). Three panels on `/dashboard`:
  live **keyword rankings**, owner-picked **target keywords** (~80, position + 28-day
  trend), and **Google index status** (per-page indexed/not via URL Inspection API).
  Setup doc: `docs/SEARCH-CONSOLE-API.md`. **Connected ✓.**
- All service pages verified technically indexable — indexing is just GSC "Request
  Indexing" + time. 5 of 7 core service pages already indexed as of this session.

## 4. Old domain redirect — DONE
- `rallyohv.com`: user swapped GoDaddy forwarding → Vercel DNS. Verified end-to-end:
  apex + www both 308 → rallyexteriorsolutions.com, valid SSL, old paths map correctly
  (e.g. `industrial-pressure-washing-*` → `/services/pressure-washing`).
  ⚠️ Domain expires **2026-07-12** — user confirmed renewal is handled.

## 5. Google Ads (Search Ads)
- **LSA not available** for Rally's category/area ("GBP not eligible") → pivoted to
  **Google Search Ads**.
- **Conversion tracking LIVE:** Google Ads account created in Expert Mode; conversion ID
  + lead label set in Vercel env; verified the tag loads on the live site. Lead form,
  call, and text clicks all fire the conversion.
- **Cleaning / pressure-washing campaign built** (in-season summer demand) in **Google
  Ads Editor**: imported `keywords.csv` (4 ad groups, 20 keywords) + `ads.csv`
  (4 responsive search ads → matching `/lp/` pages). Set Maximize Clicks / ~$4 CPC,
  location targeting, Search-only, negatives. **Posted live** (pending Google ad review
  ~1 business day). Plan: `docs/SEARCH-ADS-CLEANING.md`. Import files:
  `~/Desktop/Rally-Google-Ads/`.
- Paused the redundant **"Power Washing"** and off-season **"Christmas Lights"** campaigns
  the user had started, so only **"Cleaning - Search"** runs.

## Next up
- **Permanent + Christmas lighting Search Ads** (starting next). Plan ready:
  `docs/SEARCH-ADS-PERMANENT-LIGHTING.md`; landing page `/lp/permanent-lighting`;
  conversion tracking already live (same account).
- Ongoing user-side: GBP description/services/posts/review replies; Search Console
  **Request Indexing** for the new combo pages + guides.
